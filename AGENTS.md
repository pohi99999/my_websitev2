# 🤖 AI Agents Guidelines
<!-- n8n-as-code-start -->
<!-- n8nac-version: 2.1.2-next.4 -->

## n8n-as-code Context Root

This file is generated by `npx --yes n8nac@next update-ai`. It is bootstrap context only, not a configuration source of truth.

- Context root: `f:\my_websitev2`
- n8n version at generation time: Unknown
- n8nac command: `npx --yes n8nac@next`
- n8n-manager command: `npx --yes @n8n-as-code/n8n-manager@next`
- n8n knowledge command: `npx --yes n8nac@next skills`

Run workspace commands from this context root. Do not `cd` into the n8n-as-code source repository, n8n-manager source repository, plugin directory, or package directory to run `npx --yes n8nac@next workspace ...`, `npx --yes n8nac@next list`, `npx --yes n8nac@next pull`, `npx --yes n8nac@next push`, or `npx --yes n8nac@next update-ai`.

---

## Required Local Agent

A VS Code and GitHub Copilot-compatible agent is generated here:

- `.github/agents/n8n-architect.agent.md`

A portable skill fallback is also generated for runtimes that do not read `.github/agents`:

- `.agents/skills/n8n-architect/SKILL.md`

If your agent runtime supports workspace agents, use the `.github/agents/*.agent.md` file. If it supports skills instead, load the skill file. Otherwise, treat these files as mandatory instructions.

---

## Source Of Truth

Do not infer configuration from this file. It intentionally avoids storing the effective instance, project, sync folder, or workflow directory.

n8nac backend resolution remains the only source of effective workspace state.
- Workspace environments live in `n8nac-config.json` and are managed by `npx --yes n8nac@next env ...`.
- Managed local runtime state and secrets live in n8n-manager storage and are managed by `npx --yes @n8n-as-code/n8n-manager@next ...`.
- The effective context is resolved by the backend.

Before any n8n workflow command, run migration dry-run first, then workspace status only after migration is not required or has been applied:

```bash
cd f:\my_websitev2
npx --yes n8nac@next workspace migrate --json
npx --yes n8nac@next workspace status --json
```

Use the returned `workflowDir` exactly as provided. Do not reconstruct paths from raw config files.

---

## Safe Commands

- Primary workspace, environment, sync, validation, push, and pull work: `npx --yes n8nac@next ...`
- Local managed runtime lifecycle and tunnels only: `npx --yes @n8n-as-code/n8n-manager@next ...`
- Workspace status and migration: `npx --yes n8nac@next workspace ...`
- Workflow sync and validation: `npx --yes n8nac@next ...`
- Node knowledge and schema lookup: `npx --yes n8nac@next skills ...`

Never write `n8nac-config.json`, `~/.n8n-manager`, or n8n-manager secret files by hand.
<!-- n8n-as-code-end -->
